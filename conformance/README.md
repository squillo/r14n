# /conformance — RLPS conformance suite (planned)

Test vectors + expected decisions a third-party resolver must pass to claim an RLPS
conformance level (§9): minimal-viable / configured / comprehensive-with-provenance.
Seeded from the reference resolver's unit tests; formalized as language-neutral
JSON test vectors in a follow-up.
